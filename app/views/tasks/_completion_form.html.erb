<%= form_with(model: task) do |form| %>
  <%= form.hidden_field :completed, value: !task.completed %>
  <%= form.submit task.completed ? "Unarchive" : "Archive", class: "btn btn-primary float-right" %>
<% end %>
