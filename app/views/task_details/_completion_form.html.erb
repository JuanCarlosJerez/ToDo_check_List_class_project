<%= form_with(model: task_detail) do |form| %>
  <%= form.hidden_field :completed, value: !task_detail.completed %>
  <%= form.submit task_detail.completed ? "Uncheck" : "Check off", class: "btn btn-primary float-right" %>
<% end %>
